// Configuration Template for MXChip Hardware
// Copy this file to config.h and fill in your actual values
// config.h is in .gitignore and will NOT be committed to git

#ifndef CONFIG_H
#define CONFIG_H

// WiFi Configuration
#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"

// Proxy Server Configuration
// Proxy host can be a hostname (recommended for hosted backends) or an IPv4 address
// Examples:
//  - Hosted backend: "my-backend.onrender.com"
//  - Local server: "192.168.1.100"
// Provide port number in PROXY_SERVER_PORT (80 for HTTP or 443 for HTTPS; for HTTPS additional changes are required)
// Find your IP: ipconfig (Windows) or ifconfig (Mac/Linux)
#define PROXY_SERVER_HOST "mentalhealthbacknd.onrender.com"  // Hosted backend domain (HTTP)
#define PROXY_SERVER_PORT 80  // HTTP port
#define PROXY_ENDPOINT "/sensor-data"

// Firebase Configuration (for reference - actual connection is via proxy)
#define FIREBASE_HOST "your-project-default-rtdb.firebaseio.com"
#define FIREBASE_PROJECT_ID "your-project-id"

// Device Configuration
#define DEVICE_ID "MXCHIP_001"  // Change this to unique device ID
#define FIREBASE_UPDATE_INTERVAL_MS 2000  // Send data every 2 seconds

#endif // CONFIG_H

